version: "3.9"

services:
  api:
    build: .
    container_name: yt-downloader-api
    ports:
      - "8000:8000"
    depends_on:
      - redis

  worker:
    build: .
    command: celery -A worker.celery worker --loglevel=info
    depends_on:
      - redis

  redis:
    image: redis:7 